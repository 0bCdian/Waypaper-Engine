// This file is no longer needed - the preload script directly exposes
// the API via contextBridge. The renderer accesses it via window.API_RENDERER.goDaemon.
//
// Kept as an empty export for any remaining import references during migration.
export {};
